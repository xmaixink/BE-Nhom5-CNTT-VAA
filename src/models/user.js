class Student {
      constructor(firstName, email, lastName) {
            this.email = email;
            this.firstName = firstName;
            this.lastName = lastName;
      }
}

module.exports = Student;